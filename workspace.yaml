load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: user_code1
      port: 4000
      location_name: "user_code1"

  - grpc_server:
      host: user_code2
      port: 4000
      location_name: "user_code2"
